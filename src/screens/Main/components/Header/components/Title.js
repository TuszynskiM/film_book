import React from 'react';
import Box from '@material-ui/core/Box';

const Title = () => (
    <Box
        fontSize={70}
        fontFamily='Bangers'
        pl={2}
        style={{color: '#E60B0B', letterSpacing: 8, fontWeight: 'bold', paddingTop: 15}}
    >
      Filmbook
    </Box>
);

export default Title;